import json
from project.src.Controller.global.state import state

from addNewMission import AddNewMission

def changeMissionFiles (changeType, missionType, missionID, options=None):
    if (changeType == 'add'):

        addNewMission = 

    elif (changeType == 'modify'):

    elif (changeType == 'completed' or changeType == 'done'):

    elif (changeType == 'delete'):

def commitChangesToTasks():

def commitChangesToQuests():

def commitChangesToHabits():


